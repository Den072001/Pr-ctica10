Problema 1:
//abrir librerías//
#include <stdio.h> 
void main() 
{ 
 int N, CONT, AS; 
 AS <= N; 
 CONT<=1; 
 printf("TECLEA UN NUMERO:\n "); 
 scanf("%i",&N); 
 while(CONT<=N) 
 { 
 AS=(AS+CONT); 
 CONT=(CONT+2); 
 } 
 printf("\nEL RESULTADO ES %i\n", AS); 
} 
En este ejercicio, se realiza una sucesión de Fibonacci, el problema estaba en el As y en  un printf , esto lo solucioné agregando: \n, para que guardara la variable y se ejecutara correctamente. 

Problema 2: 
//abrir  librerías// 
#include <stdio.h>

//bloque de variables//

void main()
{
	int i, j;
	for(i=1; i<=10; i++)
	{
		printf("\nTabla del %i\n", i);
		for(j=1; j==10; j++)
		{
			printf("%i X %i = %i\n", i, j, i*j);
		}
	}
}
En este ejercicio nos tenía que dar las tablas de 1 al 10, pero la del 10 no aparecía, entonces lo corregí  agregando el símbolo < en el i=10, de modo que quedo i<=10. 

Problema 3: 
//abrir librerías//
#include <stdio.h>
#include <math.h>

//bloque de variables//
void main()
{
	int K, X, AP, N;
	float AS;
	printf("EL TERMINO GENERICO DE LA SERIE ES: X^K/K!");
	printf("\nN=");
	scanf("%d", &N);
	printf("X=");
	scanf("%d", &X);
	K=0;
	AP=1;
	AS=0;
	while(K<=N)
	{
		AS=AS+pow(X,K)/AP;
		K=K+1;
		AP=AP*K;
	}
	printf("SUM=%le",AS);
}
En este ejercicio, nos servía para obtener el enésimo término de una  sucesión, el  problema estaba en quen no guardaba los datos, entonces le agregué el símbolo & a un costado del scanf y listo. 
